{
  "name": "median-adapter",
  "version": "0.1.0",
  "author": "",
  "description": "Adapter for getting the median of multiple API's",
  "license": "MIT",
  "scripts": {
    "test": "./node_modules/.bin/_mocha --timeout 0",
    "start": "node app.js"
  },
  "dependencies": {
    "@chainlink/external-adapter": "^0.2.3",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "express": "^4.17.0",
    "mocha": "^7.1.2"
  }
}
